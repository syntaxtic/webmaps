import Basemap from '@/components/Basemap';
import Electoral from '@/components/Electoral';

const Home = () => {
  return (
    <main>
      <h1>Web Maps: Open source & Serverless</h1>
      <Basemap />
      <Electoral />
    </main>
  );
};

export default Home;
